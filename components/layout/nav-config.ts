// Re-exports from lib/navigation for backward compatibility.
// Prefer importing directly from '@/lib/navigation'.
export { navItems, bottomNavItems } from '@/lib/navigation'
export type { NavItem } from '@/lib/navigation'
